main
var a,b;
var c,d;

function foo(p1,p2);
array[4] l2;
{
   let l2[2] <- p1 * p2; 
};

function bar(p1,p2,p3,p4);
{
    call foo( p1, p3);
};
{
    while a >= b do
	if a <= b then
	    let c <- b - c * ( 1 + 2 );
        fi;
    od;
    if a <= b then
	let c <- b - c - e - d;
    fi;
    let a <- b / c;
    return
}.
